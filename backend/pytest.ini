; Pytest configuration — points it at the tests/ directory and enables
; pytest-asyncio's "auto" mode so async test functions work without a
; @pytest.mark.asyncio decorator on each one.
[pytest]
testpaths = tests
asyncio_mode = auto
